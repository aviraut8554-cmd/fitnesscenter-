import { PageHeading } from '@/components/ui';
import { SectionPlaceholder } from '@/components/section-placeholder';

export default function SettingsPage() {
  return (
    <div>
      <PageHeading title="Settings" subtitle="Business profile and branding" />
      <SectionPlaceholder note="Business profile, branding and plan settings land in a later phase." />
    </div>
  );
}
